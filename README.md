# stocksensei

Run the frontend locally:

```bash
npm install
npm run dev
```

Open the local URL printed by Vite.

For frontend-only development, create or update `.env.local` in the project root as needed.

## Docs & Support

Project-specific docs live in the repo. For issues, open an issue in the project tracker.
